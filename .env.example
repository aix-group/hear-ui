# ============================================================================
# HEAR UI - Environment Configuration (Example)
# ============================================================================
# Copy this file to `.env` and fill in secure values for local development.
#
# IMPORTANT: Never commit the actual .env file with real secrets!
# Generate secure keys with: python -c "import secrets; print(secrets.token_urlsafe(32))"
# ============================================================================

# ----------------------------------------------------------------------------
# Project Information
# ----------------------------------------------------------------------------
PROJECT_NAME=HEAR UI
STACK_NAME=hear-ui
DOMAIN=localhost
ENVIRONMENT=local

# ----------------------------------------------------------------------------
# Docker Configuration
# ----------------------------------------------------------------------------
DOCKER_IMAGE_BACKEND=hear-backend
DOCKER_IMAGE_FRONTEND=hear-frontend
TAG=local

# ----------------------------------------------------------------------------
# PostgreSQL Database
# ----------------------------------------------------------------------------
# When running with Docker Compose, use POSTGRES_SERVER=db (container name)
# For local development without Docker, use POSTGRES_SERVER=localhost or 127.0.0.1
POSTGRES_SERVER=db
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme_secure_password_here
POSTGRES_DB=hear_db

# ----------------------------------------------------------------------------
# Application URLs & CORS
# ----------------------------------------------------------------------------
# Frontend URL (used by backend for CORS and redirects)
FRONTEND_HOST=http://localhost:5173

# Backend CORS Origins (comma-separated list)
# Add all origins that should be allowed to access the API
BACKEND_CORS_ORIGINS=http://localhost:5173,http://localhost

# ----------------------------------------------------------------------------
# Security & Authentication
# ----------------------------------------------------------------------------
# Application secret key (used for internal signing; MUST be changed in production!)
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=replace-with-secure-random-value-minimum-32-chars

# First superuser placeholder (required by config schema; authentication is not
# exposed in the UI — these values are stored but no login endpoint is active)
FIRST_SUPERUSER=admin@hear-project.de
FIRST_SUPERUSER_PASSWORD=ChangeMe123!SecurePassword

# ----------------------------------------------------------------------------
# AI Model Configuration (Optional for MVP)
# ----------------------------------------------------------------------------
# MODEL_PATH=/app/models/cochlear_model.pkl
# SHAP_BACKGROUND_SAMPLES=100

# ----------------------------------------------------------------------------
# Observability & Monitoring (Optional)
# ----------------------------------------------------------------------------
# Sentry DSN for error tracking (leave empty to disable)
SENTRY_DSN=

# ----------------------------------------------------------------------------
# Email Configuration (Optional - not required for MVP)
# ----------------------------------------------------------------------------
# SMTP_HOST=
# SMTP_USER=
# SMTP_PASSWORD=
# EMAILS_FROM_EMAIL=noreply@hear-project.de
# SMTP_TLS=True
# SMTP_SSL=False
# SMTP_PORT=587

# ----------------------------------------------------------------------------
# Development Notes
# ----------------------------------------------------------------------------
# 1. For Docker Compose: Keep POSTGRES_SERVER=db
# 2. For local backend dev: Set POSTGRES_SERVER=localhost and ensure PostgreSQL is running
# 3. Protect .env file: chmod 600 .env
# 4. Never commit .env to version control (it's in .gitignore)
# ============================================================================

